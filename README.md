This example demonstrate different configuration options for mocha tests

To run the tests clone the repository and do

````
npm install -g attester

npm install

attester bdd.yaml
attester tdd.yaml
````